export class Track {
  constructor(type, id, artwork, title, creator, link) {
    this.type = type;
    this.id = id;
    this.artwork = artwork;
    this.title = title;
    this.creator = creator;
    this.link = link
  }
}